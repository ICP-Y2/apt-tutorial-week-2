# Tutorial 2 - Learning Logs (Terminal + DAO + MySQL)

## Tasks

1. Move project into MySQL database (Terminal).
2. Implement TopicDAO (Add & View Topics).
3. Implement EntryDAO (Add & View Entries).

### Bonus
- View entries of a certain topic.

## SQL Files

- Run sql/init.sql first
- Then run sql/seed.sql

## Build & Run

mvn clean compile
mvn exec:java
